rootProject.name = 'autoconfig'

include "api"
include "compiler"

// samples

include "sample:config"
include "sample:main"

project(':api').name = 'autoconfig'
project(':compiler').name = 'autoconfig-compiler'
