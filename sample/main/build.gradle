apply from: "$rootDir/gradle/java.gradle"

apply plugin: 'application'

mainClassName = "io.polymorphicpanda.autoconfig.sample.Main"

dependencies {
    compile project(":sample:config")
    compile project(":autoconfig-compiler")
    compile project(":autoconfig")
}
